# Relative so requests go through the Vite dev proxy (see vite.config.ts) and stay
# same-origin. Pointing this at http://127.0.0.1:12566 directly makes every call
# cross-origin, and the session cookie would then be dropped.
VITE_API_BASE_URL=/api
